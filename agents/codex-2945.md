## Summary
- add `agents:paused` gating to the keepalive sweep so paused PRs are skipped and reported
- introduce per-PR keepalive workflow with sticky status comment and pause-aware guard
- extend tests and docs to cover pause/resume controls and paused summary reporting

## Validation
- _pending_
