"""Utility helpers for :mod:`trend_analysis`.

The ``util`` package exposes small helper functions used across the
codebase.  New modules should keep dependencies minimal so they remain
light‑weight and importable in isolation.
"""
