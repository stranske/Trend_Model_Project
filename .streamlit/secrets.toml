# Streamlit secrets TEMPLATE.
# --------------------------------------------------------------------
# This file is meant as an example only and MUST NOT contain real keys.
#
# Recommended usage:
#   1. Copy this file to a local, git-ignored secrets file, for example:
#        .streamlit/secrets.local.toml
#      or:
#        .streamlit/secrets.toml   (ensure this path is in .gitignore)
#   2. Put your real API keys in that local file only.
#   3. Never commit real credentials to the repository.
#
# All values below are placeholders.

# Preferred key name for Streamlit deployments:
TS_STREAMLIT_API_KEY = "YOUR_OPENAI_API_KEY"
# Back-compat fallback (optional):
OPENAI_API_KEY = "YOUR_OPENAI_API_KEY"
# Optional: LLM proxy URL + token for shared deployments
TS_LLM_PROXY_URL = "https://your-llm-proxy.example.com"
TS_LLM_PROXY_TOKEN = "YOUR_PROXY_TOKEN"
# Optional: uncomment and set if you use Trend LLM
# TREND_LLM_API_KEY = "YOUR_TREND_LLM_API_KEY"
