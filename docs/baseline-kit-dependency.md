# app-baseline-kit dependency

Trend_Model_Project (the pilot repo for the baseline kit) uses **Pattern A**. `app-baseline-kit` is declared unpinned `@main` in `pyproject.toml` (`[project.optional-dependencies].dev`, with a comment at line ~114 stating intent to move to a tagged ref once the package is versioned). Pattern A is correct for a pilot package still iterating on Workflows `main`. At test time the package is installed via `pip install -e ".[dev]"` (in `baseline-report.yml`) and the reusable CI delegates to `stranske/Workflows@main`; the dependency-alignment test exempts PEP 508 direct references. Optional cleanups: the explicit git install in `baseline-report.yml` line ~44 is redundant with `.[dev]`, and a tracking issue should migrate to a tagged version once Workflows releases the package. No item-2 change needed.
