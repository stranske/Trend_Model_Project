name: Agents 75 Keepalive on Gate (disabled)

on:
  workflow_run:
    workflows: ["Gate"]
    types:
      - completed

jobs:
  keepalive-disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Gate keepalive listener disabled in favour of orchestrator sweep."
