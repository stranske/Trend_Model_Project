name: autofix trivial issues (ARCHIVED 2025-09-20 – use reuse-autofix.yml)

# ARCHIVED WORKFLOW
# Replaced by `reuse-autofix.yml` + `autofix-consumer.yml`.
# Triggers disabled; retained for historical reference until full removal.
on: {}
permissions: {}
jobs:
  archived-placeholder:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived legacy autofix workflow"
