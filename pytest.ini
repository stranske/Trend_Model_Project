[pytest]
markers =
    smoke: smoke tests for app launch
    slow: marks tests as slow (deselect with '-m "not slow"')
addopts = -ra
markers =
    quarantine(reason): known failing or flaky; excluded from required checks
    slow: long-running; excluded from required checks
