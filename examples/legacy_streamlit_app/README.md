# Legacy Streamlit app

This directory contains the previous multipage Streamlit prototype that lived
under `app/streamlit/`. The active repository layout now omits the `app/`
package entirely, so these files live here purely for historical reference and
will not receive further updates. New work should target the `streamlit_app/`
package.
