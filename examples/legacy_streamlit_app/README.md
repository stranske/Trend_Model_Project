# Legacy Streamlit app

This directory contains the previous multipage Streamlit prototype that lived
under `app/streamlit/`. It is preserved for reference only and will not receive
further updates. New work should target the `streamlit_app/` package.
