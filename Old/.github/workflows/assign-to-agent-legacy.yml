name: LEGACY Codex Assign (Disabled Stub)

# This stub intentionally does nothing. Original bloated workflow removed.
# Retain for <= 7 days only. After validation of codex-assign-minimal.yml delete this file.
# Retrieval: use git history to view original content if required.

on:
  workflow_dispatch:
    inputs:
      note:
        description: "Legacy disabled stub (no jobs)."
        required: false

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy stub notice
        run: echo "This legacy workflow is intentionally inert. Use codex-assign-minimal.yml instead."
